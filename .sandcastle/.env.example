# OpenCode API key
OPENCODE_API_KEY=

# Optional terminal mirror for file-logged automated runs: off|verbose.
# Leave blank/off for durable logs only; set verbose to also print prefixed lifecycle and agent stream output.
SANDCASTLE_TERMINAL_OUTPUT=
