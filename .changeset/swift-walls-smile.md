---
"@ecology91/sandcastle": patch
---

Add `SANDCASTLE_TERMINAL_OUTPUT=verbose` for top-level `run()` sessions so file-logged runs keep their durable log and also show prefixed lifecycle output in the terminal.
